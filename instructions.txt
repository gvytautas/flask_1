<navigate to your projects>
git clone <url>
cd <project_name>
python -m venv <virtual_environment_name>
<open project <project_name> directory with your IDE (Pycharm)>

pip freeze > requirements.txt

(git status)
git add .
git commit -m "comment"
git push